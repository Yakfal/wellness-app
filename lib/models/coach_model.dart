class Coach {
  final String name;
  final String specialty;
  final String imageUrl; // We'll use URLs for profile pictures

  const Coach({
    required this.name,
    required this.specialty,
    required this.imageUrl,
  });
}