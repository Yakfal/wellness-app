name: wellness_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  go_router: ^14.1.2
  provider: ^6.1.2
  firebase_core: ^3.2.0
  cloud_firestore: ^5.1.0
  intl: ^0.19.0
  firebase_auth: ^5.1.2 # <-- This is the new package
  fl_chart: ^0.68.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true